---
---
[
{% for faq in site.faq %}
{
 "question" : "{{ faq.question }}",
 "url" : "{{ faq.url | remove_first:'/' }}",
 "markdown" : "{{ faq.path | replace: '_faq/', ''}}" 
}{% unless forloop.last %},{% endunless %}{% endfor %}
]
